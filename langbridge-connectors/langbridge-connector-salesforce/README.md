# Langbridge Salesforce Connector

This package owns the Salesforce connector plugin for Langbridge.

It preserves the runtime behavior that previously lived in core `langbridge`
while moving ownership to the package/plugin surface.

Supported resources:

- `accounts`
- `contacts`
- `leads`
- `opportunities`
