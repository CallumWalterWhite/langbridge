# Langbridge Google Analytics Connector

This package owns the Google Analytics connector plugin for Langbridge.

It preserves the runtime behavior that previously lived in core `langbridge`
while moving ownership to the package/plugin surface.

Supported resources:

- `events`
- `pages`
- `sessions`
- `traffic_sources`
